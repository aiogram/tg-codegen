{{ entity.name|header('#') }}
{{ entity.name }}
{{ entity.name|header('#') }}

Returns: :obj:`{{ entity.python_returning_type }}`

.. automodule:: aiogram.methods.{{ entity.pythonic_name }}
    :members:
    :member-order: bysource
    :undoc-members: True


Usage
=====

As bot method
-------------

.. code-block::

    result: {{ entity.python_returning_type }} = await bot.{{ entity.pythonic_name }}(...)


Method as object
----------------

Imports:

- :code:`from aiogram.methods.{{ entity.pythonic_name }} import {{ entity.name|class_name }}`
- alias: :code:`from aiogram.methods import {{ entity.name|class_name }}`

In handlers with current bot
----------------------------

.. code-block:: python

    result: {{ entity.python_returning_type }} = await {{ entity.name|class_name }}(...)

With specific bot
~~~~~~~~~~~~~~~~~

.. code-block:: python

    result: {{ entity.python_returning_type }} = await bot({{ entity.name|class_name }}(...))

{% if entity.can_be_used_in_webhook -%}
As reply into Webhook in handler
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: python

    return {{ entity.name|class_name }}(...)
{%- endif %}
